#include "system.h"

void app_main(void) {
    /**
     * @brief The system_start() function initializes all hardware layers
     * and starts the application FreeRTOS tasks.
     */
    system_start();
}
